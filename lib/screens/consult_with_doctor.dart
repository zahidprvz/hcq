import 'package:flutter/material.dart';

class ConsultWithDoctor extends StatelessWidget {
  const ConsultWithDoctor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
